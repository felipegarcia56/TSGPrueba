import { RecursoResponsable } from './recurso-responsable';

describe('RecursoResponsable', () => {
  it('should create an instance', () => {
    expect(new RecursoResponsable()).toBeTruthy();
  });
});
